---
import { cn } from "../../lib/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn("rounded-xl border border-ink/10 bg-white shadow-sm shadow-ink/5", className)}>
  <slot />
</div>
